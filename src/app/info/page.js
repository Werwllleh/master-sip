import React from 'react';
import LeftBarLayout from "@/components/layouts/leftbar-layout";

export const metadata = {
  title: "MASTER SIP | Информация",
  description: "Строительство домов из SIP панелей в Поволжье",
};

const Page = () => {

  return (
    <LeftBarLayout>
      Главная информация
    </LeftBarLayout>
  );
};

export default Page;
